import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicangularbooking',
  appName: 'ionic-angular-booking',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
